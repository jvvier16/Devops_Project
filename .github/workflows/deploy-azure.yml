name: Deploy to Azure

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    name: Deployment Info
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Show Deployment Instructions
        run: |
          echo "===================================="
          echo "📦 Manual Deployment to AWS"
          echo "===================================="
          echo ""
          echo "Este workflow está deshabilitado para uso manual."
          echo ""
          echo "Para desplegar, usa los scripts locales:"
          echo "  1. ./pre-deploy-check.ps1"
          echo "  2. ./full-deploy.ps1"
          echo ""
          echo "Ver documentación: AWS_READY.md"
          echo "===================================="
