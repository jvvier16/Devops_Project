# Copia este archivo a deploy.env y ajusta si hace falta:
#   cp scripts/deploy.env.example scripts/deploy.env
#
# deploy.env está en .gitignore (no subas contraseñas a GitHub).

AWS_REGION=us-east-1
PROJECT_NAME=devops-u2

DB_USER=root
DB_PASSWORD=root
DB_NAME=proyecto_db
# Nombre en EC2 > Key pairs (Learner Lab: vockey)
KEY_PAIR_NAME=vockey

# Descarga "Download PEM" del lab; puedes guardarlo como vockey.pem
SSH_KEY_PATH=${HOME}/.ssh/vockey.pem

MYSQL_READY_TIMEOUT=300
APP_READY_TIMEOUT=600